#include <stdio.h>
#include <stdlib.h>

int main() {
    float a1, a2, x;
    printf("Esse código foi criado para verificar se dois números são maiores que zero ou não. \n\n");
    printf("Digite o primeiro número: ");
    scanf("%f", &a1);
    printf("Digite o segundo número: ");
    scanf("%f", &a2);

    if (a1, a2 > 0)
    {
        printf("\nValores são validos\n\n");
    }
    else
    {
        printf("Valores não são validos\n\n");
    }

    return 0;
}